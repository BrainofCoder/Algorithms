#include<stdio.h>
int main()
{
	long v;
	int t;
	scanf("%d",&t);
	while(t--)
	{
	    scanf("%ld",&v);
	    if(v==0)
	        printf("NO\n");
	    else if((v&(v-1))==0)
		    printf("YES\n");
	    else
		    printf("NO\n");
	}
	printf("\n");
}
