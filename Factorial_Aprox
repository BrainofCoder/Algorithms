#define PI 3.14159265358979323846
#define E 2.71828183

#include<math.h>

int Fac(int n)
{
	return sqrt(PI*n+1.0/3)*pow(n/e,n);
}

long int stirlingFactorial(int n)
{
    if (n <= 1)
        return 1;
    return sqrt(6.28*n) * pow((n/2.71), n);
}
